version: '3.8'

services:
  discord-bot:
    build: .
    container_name: ready-check-bot
    environment:
      - DISCORD_BOT_TOKEN=${DISCORD_BOT_TOKEN}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
